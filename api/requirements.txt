# Reverie-on-Cognee API. Cognee itself runs from source (PYTHONPATH to the
# topoteretes/cognee clone) using its own venv, which already provides cognee's
# heavy deps. These are the app-level deps layered on top (install into the same
# venv with: uv pip install -r requirements.txt).
fastapi
uvicorn[standard]
sqlalchemy>=2.0
pydantic-settings
python-multipart
boto3
pillow
fastembed
python-dotenv
numpy
